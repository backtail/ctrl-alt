#ifndef _DELAY_H_
#define _DELAY_H_

#include "stdint.h"

/// @brief Delay function
/// @details This function creates a delay in microseconds.
/// @param delay_us The delay in microseconds
void delay_us(uint32_t delay_us);

#endif // _DELAY_H_